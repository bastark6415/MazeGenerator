<Window x:Class="MazeGenerator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MazeGenerator"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        mc:Ignorable="d"
        Title="Maze Generator" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <DockPanel Grid.Column="0" LastChildFill="True">
            <!--Load,Save,Export-->
            <Border Margin="10" BorderThickness="1" Background="GhostWhite" BorderBrush="LightGray"
                    DockPanel.Dock="Top">
                <StackPanel Margin="10">
                    <Button x:Name="ButtonLoad" Content="Load" HorizontalAlignment="Stretch"/>
                    <Button x:Name="ButtonSave" Content="Save" HorizontalAlignment="Stretch"
                            Margin="0, 10"/>
                    <Button x:Name="ButtonExport" Content="Export" HorizontalAlignment="Stretch"/>
                </StackPanel>
            </Border>
            <!--Generating-->
            <Border Margin="10, 0, 10, 10" BorderThickness="1" Background="GhostWhite" BorderBrush="LightGray"
                    DockPanel.Dock="Top">
                <StackPanel Margin="10">
                    <xctk:IntegerUpDown AllowSpin="True" AllowTextInput="True"
                                        Increment="1" Minimum="1" Maximum="65000"
                                        MinWidth="75"
                                        Watermark="Height" x:Name="UpDownHeight"/>
                    <xctk:IntegerUpDown AllowSpin="True" AllowTextInput="True"
                                        Increment="1" Minimum="1" Maximum="65000"
                                        MinWidth="75" Margin="0, 10"
                                        Watermark="Width" x:Name="UpDownWidth"/>
                    <CheckBox Content="Show steps" IsThreeState="False" IsChecked="False"/>
                    <Button x:Name="ButtonGenerate" Content="Generate" HorizontalAlignment="Stretch" Click="ButtonGenerate_Click"
                            Margin="0, 10, 0, 0"/>
                </StackPanel>
            </Border>
            <!--Search paths-->
            <Border Margin="10, 0, 10, 10" BorderThickness="1" Background="GhostWhite" BorderBrush="LightGray">
                <DockPanel Margin="10" VerticalAlignment="Stretch" LastChildFill="True">
                    <Button x:Name="ButtonSearch" Content="Search" HorizontalAlignment="Stretch"
                            Margin="0, 0, 0, 10" DockPanel.Dock="Top"/>
                    <ListBox x:Name="ListBoxPathes">
                        <ListBoxItem  Margin="5">
                            <CheckBox Content="Path #1"/>
                        </ListBoxItem>
                        <ListBoxItem  Margin="5">
                            <CheckBox Content="Path #2"/>
                        </ListBoxItem>
                        <ListBoxItem  Margin="5">
                            <CheckBox Content="Path #3"/>
                        </ListBoxItem>
                        <ListBoxItem  Margin="5">
                            <CheckBox Content="Path ..."/>
                        </ListBoxItem>
                        <ListBoxItem  Margin="5">
                            <CheckBox Content="Path #n"/>
                        </ListBoxItem>
                    </ListBox>
                </DockPanel>
            </Border>
        </DockPanel>
        <!--Image-->
        <ScrollViewer Grid.Column="1">
            <Image ScrollViewer.HorizontalScrollBarVisibility="Auto"
                   ScrollViewer.VerticalScrollBarVisibility="Auto"
                   Source="C:\Users\bastark\Downloads\1.bmp"
                   RenderOptions.BitmapScalingMode="NearestNeighbor"
                   SnapsToDevicePixels="True"
                   x:Name="ImageMaze"/>
        </ScrollViewer>
    </Grid>
</Window>
